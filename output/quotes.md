# 工程判断金句

> 从 Codex / Claude Code 对话日志中自动提取的工程决策句和风险控制句。

## ⚙️ 工程决策

> 我会直接把这套多语言切换改成“运行时可切换”的实现，先梳理现有入口和受影响页面，然后落到两件事：文案不再被静态缓存、切换语言后统一刷新根界面和当前页面。
>
> `score=18.0` `unknown`

> 我准备只改 `DualCameraViewController`：把锁定按钮移动到原“移动位置”按钮的位置，尺寸统一成 `50x50`，并根据录制状态在两个按钮之间切换显示，避免影响现有录制链路和锁定逻辑。
>
> `score=17.5` `unknown`

> 这样后面设置和调试页都能统一接，普通确认保持绿色主按钮，危险确认统一红色主按钮。
>
> `score=16.5` `unknown`

## 🛡️ 风险控制

> - 已记录，这个更像是分段切换瞬间预览层/合成层状态切换导致，不影响录制结果。
>
> `score=18.5` `unknown`

> 可以，我会把弹框纵向定位改成“优先避让按钮”：上方空间够就放在按钮上方（弹框底部对齐按钮上边），否则放在按钮下方（弹框顶部对齐按钮下边），并始终做安全区域内的边界裁剪。
>
> `score=18.0` `unknown`

> 下一步我会先把“新主题 token”独立定义出来，尽量不影响现有页面逻辑，作为后续逐页替换的基线。
>
> `score=17.5` `unknown`

> 我会先读 `VideoPlaybackViewController.swift` 和相关 `video/view` 目录，确认当前视图结构、依赖和可复用边界，再在不改逻辑的前提下做拆分。
>
> `score=16.5` `unknown`

> 如果设备或当前多摄链路不支持，会直接降级并打日志，不影响录制继续进行。
>
> `score=16.0` `unknown`

## 💬 开发者风格

> 我先把 `VideoPlaybackSession` 的抽象边界和两个播放器控制器的重复逻辑对齐，重点看它们目前是如何传递状态、事件和全屏切换的。
>
> `score=21.0` `unknown`

> 我先看现有录像链路和模块边界，判断哪些基础层现在就该调整，哪些可以保持兼容递进实现。
>
> `score=21.0` `unknown`

> 我先补一层“等主画面缓冲帧就绪”的兜底，不再在镜头切换/刚启动那一瞬间直接失败；
>
> `score=19.5` `unknown`

> 我先改了协议模型，已经兼容 `get_record` 顶层 `code/message`。
>
> `score=18.0` `unknown`

> 我先给你补一版兼容：`total_chunks` 缺失时按单包处理，同时增强 base64 解码兼容性。
>
> `score=17.5` `unknown`

> 我先读这两个控制器和相关依赖，判断 UI、数据流和操作逻辑的重合度，以及合并成单页后会碰到的边界。
>
> `score=17.5` `unknown`

> 我先补一轮源码级自检，再把这次变更的风险点收口。
>
> `score=16.0` `unknown`
